{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "hybrid-rag.fullname" . }}-ingress
  labels:
    {{- include "hybrid-rag.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    {{- if and .Values.query.mtls.enabled .Values.certManager.certificates.ingressClient.enabled }}
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/proxy-ssl-secret: {{ printf "%s/%s" .Release.Namespace .Values.certManager.certificates.ingressClient.secretName | quote }}
    nginx.ingress.kubernetes.io/proxy-ssl-verify: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: {{ printf "%s-query.%s.svc.cluster.local" (include "hybrid-rag.fullname" .) .Release.Namespace | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: {{ .Values.ingress.paths.mcp }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "hybrid-rag.fullname" . }}-query
                port:
                  number: {{ .Values.query.port }}
          - path: {{ .Values.ingress.paths.researchStream }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "hybrid-rag.fullname" . }}-query
                port:
                  number: {{ .Values.query.port }}
          {{- if .Values.modChat.enabled }}
          - path: {{ .Values.ingress.paths.chat }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "hybrid-rag.fullname" . }}-mod-chat
                port:
                  number: 80
          {{- end }}
{{- end }}
