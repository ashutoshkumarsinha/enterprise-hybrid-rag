{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (printf "%s-sa" (include "hybrid-rag.fullname" .)) .Values.serviceAccount.name }}
  labels:
    {{- include "hybrid-rag.labels" . | nindent 4 }}
{{- end }}
