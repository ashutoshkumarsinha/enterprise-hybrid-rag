# hybrid-rag-query benchmarks — pip install -r benchmarks/requirements.txt
ragas>=0.2.14
datasets>=2.19.0
locust>=2.29.0
langsmith>=0.1.147
httpx>=0.27.0
# OpenAI-compatible client for Ragas judge — points at inference vllm_url
openai>=1.40.0
